# Shared across all four projects. Pinned to the minor version that was used
# to produce the metrics quoted in each project README.
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scikit-learn>=1.3
scipy>=1.10
lightgbm>=4.0
matplotlib>=3.7
pytest>=7.4
